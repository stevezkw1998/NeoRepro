# Synthetic plug-in example

This four-row fixture demonstrates explicit unsupported output, common-support evaluation, and patient-level ranking without using biological data.

Run the portable standard entry point with:

```bash
neorepro benchmark contracts/synthetic/standard_predictions.csv \
  --output-dir neorepro-results --bootstrap 100
```
